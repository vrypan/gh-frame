{
  "name": "readme-to-static-page",
  "version": "1.0.0",
  "description": "GitHub Action to build a static page from README.md",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @vercel/ncc build src/index.js -o dist && cp src/*.html src/*.css src/*.png dist/ || true"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "handlebars": "^4.7.8",
    "marked": "^11.1.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
